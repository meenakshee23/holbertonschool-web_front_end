## Python_variable_annotations 

# Type annotations in Python 3

A type annotation, sometimes called a type hint, is an optional notation that specifies the type of a parameter or function result.

- A special syntax in Python 3 used to explicitly declare the data type of variables, function parameters, and return values.

